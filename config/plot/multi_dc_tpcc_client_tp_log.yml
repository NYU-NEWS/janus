bench:
    - tpcc
graph:
    using_cmd: ($6):($7)
    log_scale: xy 10
    x_label: 'Clients'
    y_label: 'Throughput (txn/s)'
    y_range: '[1:]'
    x_range: '[1:]'
    output_file: 'multi_dc_${bench}_${txn}_client_tp_log'
    key_location: 'above'

