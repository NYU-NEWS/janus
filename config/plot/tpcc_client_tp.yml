bench: 
    - tpca
    - tpcc
    - rw_benchmark
graph:
    using_cmd: ($6):($7)
    x_label: 'Clients'
    y_label: 'Throughput (txn/s)'
    y_range: '[0:1200]'
    x_range: '[0:1600]'
    output_file: 'tpcc_${txn}_${bench}_client_tp'
    key_location: 'top left'
