#include "coord.h"

namespace rococo {


void TapirCoord::do_one(TxnRequest& ) {
  verify(0);
}

void TapirCoord::FastAccept() {
  verify(0);
}

void TapirCoord::FastAcceptAck() {
  verify(0);
}

void TapirCoord::Decide() {
  verify(0);
}

} // namespace rococo